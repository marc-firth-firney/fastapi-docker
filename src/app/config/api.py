
'''
|--------------------------------------------------------------------------
| API Versions And Endpoints
|--------------------------------------------------------------------------
| Duplicate the latest version in /app/api/ to create a new API version. 
| i.e. Copy /app/api/v1 -> /app/api/v2. Then add any changes to the
| available endpoints in that version of the API below.
|
'''
api = {
    "endpoints": {
        "v1": ["health"]
    }
}
